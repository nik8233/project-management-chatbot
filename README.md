
# Project Copilot — Streamlit Tracker (v3, with Chat)
- Projects CRUD in SQLite, seeded from `projects_seed.csv` (your uploaded data).
- Fixes the `deadline_bucket` error and adds Chat + Announcements tabs back.
- Optional "Modernize dates" toggle on first run to shift dates around today.

## Run
```bash
pip install -r requirements.txt
streamlit run app_tracker.py
```
